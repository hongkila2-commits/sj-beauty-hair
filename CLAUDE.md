# SJ BEAUTY HAIR 홈페이지 — 작업 규칙

## 이 저장소가 무엇인가

대전 미용실 프랜차이즈 SJ뷰티헤어의 공식 홈페이지. 고객 유치가 주목적이고,
블로그·칼럼·영상을 쌓아 검색 유입을 만드는 것이 핵심 전략이다.

## 반드시 지킬 것

1. **데이터는 `src/config.ts` 를 통해서만 쓴다.** 지점 주소·전화·예약 링크를 페이지에
   직접 써넣지 않는다. 값이 비어 있으면 해당 UI 가 숨겨지는 구조를 유지한다.

   자주 바뀌는 값(연락처·영업시간·지점 전화·예약/지도 링크)은 `content/settings/*.json`
   에 있고 관리자가 `/admin` 에서 고친다. `config.ts` 는 그 JSON 을 읽어 병합할 뿐이다.
   **구조 정보(주소·사진·자치구·slug)는 JSON 으로 옮기지 말 것** — Decap CMS 는 양식에
   없는 항목을 저장할 때 지워버리므로, 옮기면 지점 데이터가 유실될 수 있다.

2. **글은 `content/` 아래 마크다운으로만 관리한다.** `src/content/` 를 쓰지 않는다.
   이 폴더가 Obsidian 보관함이자 웹 관리자 화면의 대상이기 때문이다.

3. **글에 넣는 사진 경로는 두 가지 모두 유효하다.**
   - Obsidian: `attachments/사진.jpg` (상대경로 → Astro 가 자동 최적화)
   - 웹 관리자: `/uploads/사진.jpg` (public 폴더에서 그대로 제공)
   둘 다 동작하므로 어느 쪽도 바꾸려 하지 말 것.

4. **개인 휴대폰 번호를 공개하지 않는다.** 기업소개서 PDF 에 담당자 010 번호가 있으나
   비공개로 결정했다. 지점 대표번호만 `config.ts` 에 넣는다.

5. **한글 URL 은 의도된 것이다.** 국내 검색 노출에 유리하다. 영문 슬러그로 바꾸지 말 것.

## 설계 규칙 (추가)

- **예약 버튼은 `bookingLink()` 한 곳에서만 판단한다.** 페이지마다 조건문을 다시 쓰지 말 것.
  우선순위: 지점 예약 주소 → 지점 플레이스 → 브랜드 예약 → 브랜드 플레이스 → null(부르는 쪽이 문의로 대체).
- **팝업은 저장 실패를 견뎌야 한다.** localStorage 읽기·쓰기를 try/catch 로 감싼 상태를 유지할 것.
  시크릿 모드에서 접근만 해도 예외가 나는 브라우저가 있다.
- **카드뉴스는 마크다운 본문이 아니라 frontmatter 의 `cards` 목록**이다.
  관리 화면에서 카드를 한 장씩 추가·삭제·순서변경 할 수 있어야 하기 때문이다.
- 대비를 낮출 때 `opacity` 를 쓰지 말고 색을 직접 지정한다. 밝은 색면 위에서 글자가 안 읽힌다.
- **사진 압축은 `dist/` 에서만 한다.** `src/integrations/optimize-uploads.mjs` 는
  배포본만 줄이고 `public/uploads/` 원본은 건드리지 않는다. 원본을 직접 고치면
  빌드할 때마다 거듭 압축돼 화질이 계속 나빠지고 원본이 사라진다.
  형식·파일명도 바꾸지 않는다 — 글 본문의 `/uploads/x.jpg` 링크가 깨진다.
- **사진 한 장 때문에 배포가 막히면 안 된다.** 압축 실패는 파일 단위로 잡아
  경고만 남기고 원본을 그대로 내보낸다.

## 사이트 주소

- **주소의 출처는 `src/site.mjs` 하나뿐이다.** 도메인을 바꿀 때 다른 곳을 찾아
  고치지 말 것. 현재 도메인은 `https://sjbthair.com` (가비아, www 없이가 대표).
- `public/admin/config.yml` 에는 주소 대신 `__SITE_URL__` 표시자가 들어 있고,
  `src/integrations/inject-site-url.mjs` 가 빌드 끝에 치환한다.
  **이 파일에 절대주소를 다시 써넣지 말 것.**
- 표시자가 남으면 **빌드를 실패시킨다.** 조용히 넘어가면 base_url 이 틀린 채
  배포돼 /admin 로그인이 막힌다. 이 안전장치를 없애지 말 것.
- `npm run dev` 로 로컬에서 /admin 을 열면 표시자가 그대로 보인다. 정상이다 —
  로컬 관리자 화면은 원래 GitHub 로그인이 안 돼 쓸 수 없다.
- 도메인을 바꾸면 **GitHub OAuth App 의 Callback URL 도 함께 바꿔야** 한다.
- 도메인이 안 열린다는 말이 나오면 **추측하지 말고 `npm run domain` 을 먼저 돌린다.**
  네임서버가 어디를 가리키는지가 거의 항상 원인이다. 가비아에서 DNS 를 설정해도
  네임서버가 다른 곳이면 아무 효과가 없다.

## 관리자 로그인 (팝업 통신)

로그인의 마지막 단계는 팝업 창이 관리자 화면에 토큰을 건네주는 대화다.
여기서 한 번 조용히 고장 난 적이 있다. 아래 규칙을 지킬 것.

- **인사(`authorizing:github`)를 한 번만 보내지 말 것.** 관리자 화면이 아직 귀를
  열지 않았으면 그 인사는 허공에 사라지고 로그인이 영영 안 된다.
  답인사가 올 때까지 100ms 간격으로 반복한다.
- **타이머로 창을 닫지 말 것.** 토큰을 **보낸 뒤에만** 닫는다. 전달 전에 닫히면
  화면상으로는 창이 떴다 닫히는데 로그인은 안 된 상태가 되어 원인을 찾을 수 없다.
- **성공과 실패에 같은 문구를 쓰지 말 것.** 실패는 빨간 글씨로 **실제 사유**를
  보여주고 **창을 닫지 않는다.** 사용자가 그 문구를 읽고 전해줄 수 있어야 한다.
- **실패를 부모 창에 postMessage 하지 말 것.** Decap 은 실패 알림을 받으면 팝업을
  즉시 닫아버려서, 애써 띄운 사유를 아무도 못 읽는다.
- **`base_url` 을 `location.origin` 으로 바꾸지 말 것.** 솔깃하지만 틀렸다.
  GitHub OAuth App 은 Callback URL 을 **하나만** 가진다. 그래서 흐름이 이렇게 된다.

  ```
  /api/auth (A도메인) → 쿠키를 A에 심음 → GitHub → 등록된 Callback(B도메인)
  → /api/callback (B) 에는 A의 쿠키가 없다 → 실패
  ```

  즉 로그인은 **등록된 대표 도메인 한 곳**을 거쳐야 한다. config.yml 의
  `base_url`(= `__SITE_URL__`)을 그대로 쓰고 `public/admin/index.html` 에서
  덮어쓰지 않는다. vercel.app 에서 들어와도 로그인 창만 대표 도메인을 거칠 뿐
  토큰은 창을 넘어 전달되므로 정상 동작한다.
- **`/api/auth` 는 `redirect_uri` 를 명시한다.** 생략하면 GitHub 이 등록된 주소로
  조용히 보내버려, 위 불일치가 '창이 떴다 닫히는데 로그인은 안 됨' 으로만 나타난다.
  명시하면 GitHub 이 `redirect_uri_mismatch` 라고 대놓고 알려준다. 빼지 말 것.
- **도메인을 바꾸면 OAuth App 의 Callback URL 도 같이 바꾼다.** 안 바꾸면 로그인이
  전부 막힌다. `src/site.mjs` 를 고칠 때 함께 처리할 것.
- 고친 뒤에는 `node scripts/check-admin-login.mjs` 를 돌린다. 이 검사에는
  **예전 코드를 그대로 둔 대조군**이 있다. 대조군이 통과하기 시작하면 검사가
  무의미해진 것이니 조건을 다시 볼 것.

## 관리자 입력칸

- **입력칸을 만들면 화면에 반드시 연결한다.** config.yml 에만 칸을 만들고 페이지가
  그 값을 안 쓰면, 사장님이 고치고 저장해도 홈페이지는 그대로다. 오류도 안 난다.
  실제로 `recruitDesc`·`recruitLead`·`recruitCtaText`·`contactFormHint` 네 개가
  그 상태로 방치돼 있었다.
- **같은 문구를 코드에 또 적지 말 것.** 위 네 개는 설정 파일에 값이 있는데도
  페이지가 같은 내용을 하드코딩하고 있어서, 겉보기에 멀쩡해 문제를 못 알아챘다.
- `scripts/check-routes.mjs` 가 `content/settings/pages.json` 의 모든 문구가
  실제 화면에 나오는지 대조한다. 칸을 추가하면 이 검사가 연결을 강제한다.
- 표·목록은 고정 필드가 아니라 `list` 위젯으로 둔다. 줄을 추가·삭제·순서변경 할 수
  있어야 나중에 항목이 늘어도 코드를 안 고친다.

## 영상

- **유튜브 플레이어를 미리 심지 말 것.** iframe 하나가 1MB 가까이 끌고 온다.
  영상 9편이면 아무도 재생을 안 눌러도 9MB 다. `VideoCard.astro` 는 평소엔
  대표 이미지만 두고 **누른 뒤에** 플레이어를 만든다. 이 구조를 되돌리지 말 것.
- 주소는 `youtube-nocookie.com/embed/…?autoplay=1&rel=0&playsinline=1`.
  `playsinline` 을 빼면 아이폰에서 전체화면으로 튄다.
- **제목 링크는 유튜브로 둔다.** 거기서 보고 싶은 사람, 구독하려는 사람이 있다.
- 클릭은 카드마다 붙이지 않고 문서에서 한 번만 받는다(위임). 영상이 몇 편이든
  스크립트는 한 벌이다.
- 검사는 `scripts/check-actions.mjs` — 누르기 전 유튜브 요청이 **0건**인지까지
  본다. 이 샌드박스는 유튜브가 막혀 있어 요청을 가로채 세는 방식이다.

## 검색엔진 (SEO)

- **h1 은 페이지마다 정확히 하나.** 전 페이지에 항상 들어가는 요소(점검 안내 같은
  오버레이)에 h1 을 쓰지 말 것. CSS 로 숨겨도 HTML 에는 남아서, 모든 페이지의
  첫 h1 이 그 문구가 된다. 실제로 '시스템 점검 중입니다' 가 전 페이지의 첫 h1 이었다.
- **검사에서 숫자를 출력만 하지 말 것.** h1 개수를 몇 주 동안 찍어놓고 아무도
  안 봤다. 출력할 값이면 기대값을 정해 `problems` 에 넣는다.
- **인원수처럼 두 곳에 나오는 숫자**는 한쪽만 고치면 홈페이지에 서로 다른 값이
  동시에 보인다. `src/config.ts` 의 `COMPANY` 와 `content/settings/home.json` 의
  `stats` 가 그렇다. 한쪽을 고치면 반드시 다른 쪽도 본다.

## 점검 · 검사

- **검사는 `npm run check` 하나로 돈다.** 빌드 → 미리보기 기동 → 검사 5종 → 정리.
  스크립트를 늘리면 `scripts/check-all.mjs` 의 `SUITES` 에 추가한다.
- **브라우저 경로는 `PLAYWRIGHT_CHROMIUM` 환경변수**로 바꾼다. 하드코딩하지 말 것.
  이 개발 환경과 GitHub Actions 의 경로가 다르다.
- **가시성 판단에 `getComputedStyle` 을 쓰지 말 것.** 부모가 숨겨져도 자기 값을
  그대로 돌려줘서 거짓 통과가 난다. `checkVisibility()` 를 쓴다.
- 주간 자동 점검은 `.github/workflows/weekly-check.yml`.
  cron `0 16 * * 6` = 한국시간 일요일 01:00. **성공하면 알리지 않는다** —
  매주 '이상 없음' 이 오면 사람이 안 읽게 된다.

## 점검 안내 화면

- 정적 사이트라 서버에서 시간 분기를 할 수 없어 **브라우저에서 판단**한다.
  진짜 차단이 아니라 안내다. 대신 검색엔진에 안전하다 —
  진짜로 막으면 새벽에 들어온 봇이 페이지를 색인에서 내릴 수 있다.
- **판단 스크립트는 body 의 맨 앞**에 있어야 한다. 뒤로 옮기면 본문이 번쩍
  보였다가 덮인다. `html[data-maintenance]` 표시 + CSS 로 가리는 구조를 유지할 것.
- 검색 봇과 `?preview=1` 은 항상 통과시킨다.

## 아직 못 받은 정보 (자리만 잡혀 있음)

- 지점별 네이버 예약 주소 → 관리 화면 (플레이스 주소만 보유 중)
- 지점 대표 전화번호 → `BRANCHES[].phone`, `BRAND.phone`
- 시술 메뉴·가격표 (페이지 자체가 아직 없음)
- 고해상도 매장·스타일 사진 (현재 사진은 PDF 에서 추출한 저해상도)
- 디자이너 프로필

## 확인 방법

```bash
npm run build    # 빌드가 통과해야 한다
npm run preview  # http://localhost:4321
```

변경 후에는 최소한 `/`, `/branches`, `/blog` 를 눈으로 확인한다.
